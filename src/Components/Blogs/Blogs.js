import React from 'react';
import './Blogs.css';

const Blogs = () => {
  return (
    <div>
      <div>
        <h1 className='context-api'>What is Purpose of Context api react</h1>
        <h3 className='h3'>A response to the context API is a structure fee, which is used to pass information to the rector computer. It is a global state management, which helps  share information between different computers.In case of individual requirements, sending data between different components becomes easier so that don't have to write special code. For example, if  want to send data from a parent component to a child component, i can do so using props. But if i want to send the same data to multiple components, this process can be quite complicated.that's why Context API serves to solve this problem.</h3>
      </div>
      <div>
        <h1 className='context-api'> what is semantic tag react</h1>
        <h3 className='h3'>"Semantic tag" in the context of React refers to the use of HTML elements with specific meanings or roles that convey the structure and purpose of the content they contain. In React, as in HTML, These tags are used to build the user interface and represent different parts of the web page or application.</h3>
      </div>
    </div>
  );
};

export default Blogs;